const MobileNavViewModel={initialize(){const nav=document.querySelector("nav");if(!nav)return;const links=document.querySelector(".nav-links");if(!links)return;let button=document.getElementById("mobileMenuToggle");if(!button){button=document.createElement("button");button.id="mobileMenuToggle";button.className="mobile-menu-toggle";button.type="button";button.setAttribute("aria-controls","portfolioMobileMenu");button.setAttribute("aria-expanded","false");button.setAttribute("aria-label","Open navigation menu");button.innerHTML='<span></span><span></span><span></span>';nav.appendChild(button)}let menu=document.getElementById("portfolioMobileMenu");if(!menu){menu=document.createElement("div");menu.id="portfolioMobileMenu";menu.className="mobile-menu";menu.setAttribute("aria-hidden","true");menu.innerHTML=links.innerHTML;document.body.appendChild(menu)}const close=()=>{menu.classList.remove("is-open");menu.setAttribute("aria-hidden","true");button.setAttribute("aria-expanded","false");button.setAttribute("aria-label","Open navigation menu")};button.addEventListener("click",()=>{const open=!menu.classList.contains("is-open");menu.classList.toggle("is-open",open);menu.setAttribute("aria-hidden",String(!open));button.setAttribute("aria-expanded",String(open));button.setAttribute("aria-label",open?"Close navigation menu":"Open navigation menu")});menu.querySelectorAll("a").forEach(a=>a.addEventListener("click",close));document.addEventListener("click",e=>{if(!menu.classList.contains("is-open"))return;if(!menu.contains(e.target)&&e.target!==button&&!button.contains(e.target))close()});document.addEventListener("keydown",e=>{if(e.key==="Escape")close()})}};if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",()=>MobileNavViewModel.initialize(),{once:true});else MobileNavViewModel.initialize();